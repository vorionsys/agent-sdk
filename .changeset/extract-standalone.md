---
"@vorionsys/agent-sdk": patch
---

Extract `agent-sdk` into its own standalone repository (`vorionsys/agent-sdk`) with tokenless OIDC trusted publishing + provenance. Relicensed Apache-2.0. (Package name unchanged; a future rebrand to `@vorionsys/aurais-agent-sdk` is tracked separately.)
